import { createTheme } from "@mui/material";

export const darkTheme = createTheme({
  palette: {
    mode: "dark",

    // background: {
    //   default: "#111927",
    // },

    primary: {
      main: "#fff",
    },
  },

  typography: {
    fontFamily: "Armata",
  },
});
